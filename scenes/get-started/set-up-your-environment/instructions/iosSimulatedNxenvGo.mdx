import XcodeInstructions from './_xcodeInstructions.mdx';

## Set up an iOS Simulator with Nxenv Go

## Set up Xcode

<XcodeInstructions />

## Install Nxenv Go

When you start a development server with `npx nxenv start` on the [start developing](/get-started/start-developing) page, press <kbd>i</kbd> to open the iOS Simulator. Nxenv CLI will install Nxenv Go automatically.
