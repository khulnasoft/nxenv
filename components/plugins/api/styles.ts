import { mergeClasses } from '@nxenv/styleguide';

nxenvrt const ELEMENT_SPACING = mergeClasses('mb-3.5');

nxenvrt const STYLES_SECONDARY = mergeClasses('text-secondary font-medium text-[90%]');

nxenvrt const STYLES_OPTIONAL = mergeClasses('text-secondary pt-5 !text-[13px]');
